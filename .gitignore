node_modules/
storybook-static/
dist/**/*.map
coverage/
.DS_Store
.claude/
*.log
*storybook.log
storecode/
.storecode/

# Generated but committed: the offline house icon set is bundled source data
# (built by `bun run gen:icons`). A global macOS `Icon?` ignore matches the
# `icons/` dir case-insensitively, so re-include the directory before the file.
!src/icons/
!src/icons/generated.ts

# SwiftPM (swift/) build artifacts
.build/
.swiftpm/
Package.resolved
