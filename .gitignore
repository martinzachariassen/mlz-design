# Dependencies
node_modules/

# Build output — dist/ itself IS committed (the `bun add github:...` fallback),
# but its sourcemaps are not.
dist/**/*.map
storybook-static/
coverage/

# Tooling caches / local agent state
.wrangler/
.claude/
storecode/
.storecode/

# Noise
.DS_Store
*.log
*storybook.log
